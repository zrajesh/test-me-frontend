// Import components
import Navigation from "../components/navigation/Navigation";

const HomePage = () => {
    return (
        <div>
            <Navigation /> 
        </div>
    );
};

export default HomePage;
